name: build kernelnosu

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup zig
        uses: mlugg/setup-zig@v2
        with:
          version: master

      - name: grab sstrip from ELFkickers
        run: |
          curl -L https://www.muppetlabs.com/~breadbox/pub/software/ELFkickers-3.2.tar.gz -o sstrip.tar.gz
          tar xf sstrip.tar.gz
          make -C ELFkickers-3.2 sstrip
          cp ELFkickers-3.2/sstrip/sstrip .

      - name: compile su via zigcc
        run: |
          echo "arm, aarch64 and x86_64"
          pwd
          chmod +x sstrip
          
          cd src
          
          zig cc -target arm-linux -std=c23 -s -Oz -static -Wl,--gc-sections,-z,norelro -fno-unwind-tables -Wl,--entry=__start -flto -fmerge-all-constants -Wno-unknown-attributes su.c -o ../module/su-arm
          zig cc -target aarch64-linux -std=c23 -s -Oz -static -Wl,--gc-sections,-z,norelro -fno-unwind-tables -Wl,--entry=__start -flto -fmerge-all-constants -Wno-unknown-attributes su.c -o ../module/su-arm64
          zig cc -target x86_64-linux -std=c23 -s -Oz -static -Wl,--gc-sections,-z,norelro -fno-unwind-tables -Wl,--entry=__start -flto -fmerge-all-constants -Wno-unknown-attributes su.c -o ../module/su-x64
          
          cd ..

          wc -c module/su-*
          ./sstrip -z module/su-arm64
          ./sstrip -z module/su-arm
          ./sstrip -z module/su-x64
          wc -c module/su-*
          file module/su-*

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: kernelnosu
          path: module/

